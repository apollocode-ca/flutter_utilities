import 'package:live_tester/src/configs/theme_provider/text_theme/poppins_text_styles.dart';

final mobileDisplay = regularPoppins.copyWith(
  fontSize: 36,
  height: 44 / 36,
);

final mobileHeadline = regularPoppins.copyWith(
  fontSize: 24,
  height: 32 / 24,
);

final mobileTitle = mediumPoppins.copyWith(
  fontSize: 14,
  height: 20 / 14,
  letterSpacing: 0.1,
);

final mobileLabel = mediumPoppins.copyWith(
  fontSize: 11,
  height: 16 / 11,
  letterSpacing: 0.5,
);

final mobileBody = regularPoppins.copyWith(
  fontSize: 12,
  height: 16 / 12,
  letterSpacing: 0.4,
);
